# =========================================================================
# Sentinel · 声纹捕手前端 · 环境变量样板
# 复制为 .env.local 后填写；.env.local 不会进 git
# =========================================================================

# Gateway 地址（开发默认本机 8080）
# 生产部署时改为真实域名，如 https://api.sentinel.example
NEXT_PUBLIC_API_URL=http://localhost:8080
