# 声纹捕手前端 · 生产构建环境变量
# NEXT_PUBLIC_ 变量在 build 时编译进产物，改了必须重新 build。
#
# 同源方案：不带端口，浏览器请求 http://172.16.30.10/api/v1/... → 命中 nginx:80 → 转发到后端 8080。
# 页面和 API 同源，无 CORS，且 8080 无需对外暴露。
NEXT_PUBLIC_API_URL=http://172.16.30.10
